spring:
  application:
    name: auth-server
  datasource:
    url: jdbc:h2:file:~/auth-server-poc;DB_CLOSE_ON_EXIT=TRUE
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

server:
  port: 9090